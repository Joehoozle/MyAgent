(define agent-power-average 0)
(define agent-power-max 0)
(define agents-seen 0)

; I do not think that moving agressively is nescessary except maybe when going to eat from a vegetation
; Power should only really come into play during an eat phase



; if my agent does not eat, it will and wait for the next bloom cycle
